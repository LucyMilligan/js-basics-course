// function that returns true if width > height and its landscape

function isLandscape(width, height) {
    // return (width > height) ? true : false;
    return (width > height);
}

const artIsLandscape = isLandscape(200, 50)
console.log(artIsLandscape)